# RabbitMQ Game Configuration
# Client-facing game traffic queue

default_user = game
default_pass = change-me-rmq-game
default_vhost = /

# TLS for client connections
listeners.ssl.default = 31982

# Management plugin (internal only)
management.listener.port = 15673
management.listener.ip = 0.0.0.0
