; Dune Awakening - Gateway Configuration
; Controls how the server registers with Funcom Live Services
;
; NOTE: [OnlineSubsystem] ServerName and DatacenterId are injected
; at container startup via the docker-compose entrypoint command.
; See docker-compose.yml gateway service for details.

[Gateway]
; Provider name shown in server info
Provider=self-hosted
