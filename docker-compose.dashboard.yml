name: dune-awakening-dashboard

services:
  dashboard-api:
    extends:
      file: docker-compose.yml
      service: dashboard-api

  dashboard-frontend:
    extends:
      file: docker-compose.yml
      service: dashboard-frontend
